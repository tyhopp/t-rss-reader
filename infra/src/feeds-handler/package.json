{
  "private": "true",
  "name": "feeds-handler",
  "description": "Lambda function that handles CRUD operations for the feeds DynamoDB table",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "//": "Create the zip adjacent to the source",
    "clean": "rm ../feeds-handler.zip",
    "build": "zip ../feeds-handler.zip index.mjs"
  }
}
